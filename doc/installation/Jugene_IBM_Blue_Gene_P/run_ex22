# @ job_name = petsc_test

# @ error = $(job_name).$(jobid).out

# @ output = $(job_name).$(jobid).out

# @ environment = COPY_ALL

# @ wall_clock_limit = 00:10:00

# @ notification = never

# @ job_type = bluegene

# @ bg_size = 32

# @ queue

mpirun -exe ./ex22 -mode SMP -args "-dmmg_nlevels 4 -ksp_monitor -ksp_view -da_grid_x 15 -da_grid_y 15 -da_grid_z 15 -mg_coarse_pc_type lu -mg_coarse_pc_factor_mat_solver_package superlu_dist -log_summary"
